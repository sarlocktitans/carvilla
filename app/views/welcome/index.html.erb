<% if !current_user %>
	<%= link_to "Login", user_session,
					class: "btn btn-danger btn-md" %> 
<% else %>
	<%= link_to "Logout", destroy_user_session_path, method: :delete,
						class: "btn btn-danger btn-md" %> 
<% end %>